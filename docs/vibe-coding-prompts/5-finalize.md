# Finalize Feature

Finalize **[FEATURE NAME]**:

1. Run `pnpm lint` and `pnpm type-check` - fix any errors
2. Review all implementation work from the various tasks built for the feature, and update `docs/Features/[feature-name].md` with any relevant detail for documentation.
2. Update `docs/changelog.md` with what was added/changed
3. Update `docs/project_status.md` to mark feature complete
4. Add any architectural decisions to `docs/decision_log.md`
5. Prepare a conventional commit message

Start with step 1.

